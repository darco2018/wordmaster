# doesn't use it. Instead testing generates its own name for db (with @AutoConfigureTestDatabase included in @DataJpaTest)
spring.datasource.url=jdbc:h2:mem:test
spring.h2.console.settings.trace=true
